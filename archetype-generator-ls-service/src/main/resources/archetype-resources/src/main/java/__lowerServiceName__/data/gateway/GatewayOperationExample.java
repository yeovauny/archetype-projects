package ${groupId}.${lowerServiceName}.data.gateway;

import ${groupId}.${lowerServiceName}.domain.gateway.Gateway;
import org.springframework.stereotype.Component;

@Component
public class GatewayOperationExample  implements Gateway {

    /**
     * this method is used to call the another service or database or external operations
     *
     * */

    @Override
    public void methodExampleCaller() {



    }

}
